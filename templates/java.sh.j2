#!/usr/bin/env bash
# {{ ansible_managed }}

export JAVA_HOME="{{ java_home }}"

if [ $(echo $PATH | grep -c "${JAVA_HOME}/bin") -eq 0 ]
then
  PATH="${PATH:+${PATH}:}${JAVA_HOME}/bin"
  export PATH
else
  export PATH
fi
